# Run Monarch native suites through the rootfs

Type: task
Status: ready-for-agent
Blocked by: 02
Parent: ../spec.md

## Requirements

- Run the approved Python, Rust, build, documentation, and control-plane
  subsets required by the repository release contract through `scripts/run`.
- Produce fresh machine-readable results bound to exact identities.

## Verification

- Focused and broader required checks pass from a clean checkout.

## Comments
