# CLAUDE.md

@CONSTITUTION.md

For repository-specific guidance, build commands, code style, testing, and
configuration, follow `AGENTS.md` at the repository root.

The imported constitution is pinned to a reviewed release tag (`v2026.08.11`).
Update it through normal pull-request review, not by fetching a newer version at
startup.
