# @generated by autocargo from //monarch/torch-sys-cuda:torch-sys-cuda

[package]
name = "torch-sys-cuda"
version = "0.0.0"
authors = ["Meta"]
edition = "2024"
license = "BSD-3-Clause"
links = "torch_cuda"

[dependencies]
derive_more = { version = "2.1.1", features = ["full"] }
fxhash = "0.2.1"
monarch_types = { version = "0.0.0", path = "../monarch_types" }
nccl-sys = { version = "0.0.0", path = "../nccl-sys", optional = true }
pyo3 = { version = "0.27", features = ["anyhow", "multiple-pymethods", "py-clone"] }
thiserror = "2.0.20"
torch-sys2 = { version = "0.0.0", path = "../torch-sys2" }

[build-dependencies]
build_utils = { version = "0.0.0", path = "../build_utils" }

[features]
cuda = ["dep:nccl-sys"]
default = []

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(fbcode_build)"]}
